---
state_backends:
  - name: s3-east1
    type: s3
    encrypt: true    # server-side encryption of the state objects (stage 39; re-init with -reconfigure)
    bucket: my-east1-tfstate-bucket
    key: statefiles/csia/ # Automatically appends the rest of the path based on the builder
    region: us-east-1
    profile: noaa
