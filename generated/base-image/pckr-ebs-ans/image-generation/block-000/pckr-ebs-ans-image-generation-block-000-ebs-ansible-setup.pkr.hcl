# Additional setup for Packer EBS Ansible Builder generated by PackerEbsImageBuilder (pckr-ebs-ans)
locals {
  snapshot      = var.release ? "" : "-SNAPSHOT"
  image_version = "${var.base_image_version}${local.snapshot}"
}