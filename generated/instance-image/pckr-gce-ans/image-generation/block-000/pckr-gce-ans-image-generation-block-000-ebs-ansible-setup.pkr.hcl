# Additional setup for Packer EBS Ansible Builder generated by PackerGceImageBuilder (pckr-gce-ans)
locals {
  snapshot      = var.release ? "" : "-SNAPSHOT"
  image_version = "${var.base_image_version}${local.snapshot}"
}